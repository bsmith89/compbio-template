# Just to shut-up that damned warning message.
pip>=6.1.1

# Standard part of projects, but not _entirely_ necessary.
ipython
jupyter
matplotlib
rpy2
pandas
seaborn
numpy
statsmodels

# Common tools for working with sequence data
# -e flag makes it editable:
# installing with `pip install --src packages -r requirements.txt`
# will result in a local copy of the repository in that directory.
-e git+https://github.com/bsmith89/compbio-scripts.git#egg=sequtils

# For generating makefile figures.
networkx==1.9.1
git+https://github.com/pygraphviz/pygraphviz.git#egg=pygraphviz
